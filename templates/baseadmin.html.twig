<!DOCTYPE html>
    <html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="{{ asset('font/css/font-awesome.min.css') }}"  rel="stylesheet">
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        <link rel="icon" href="{{ asset('icons/favicon.ico') }}">
        <script type="text/javascript" src="{{ asset('js/burger.js') }}" defer></script>

        <title>{% block title %}Admin Parking-Rue-Du-Moulin{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}

    </head>
<body>
<header>
    <p id="burger" class="burger"></p>
    <h1><a href="{{ path('app_admin') }}">Gestion Parking</a></h1>
    <nav>
        <ul>
            <li><a href="{{ path('app_admin') }}">Réserver</a></li>
            <li><a href="{{ path('app_admin_planning') }}">Planning</a></li>
            <li><a href="{{ path('app_admin_planning_jour', {date: "now"|date("Y-m-d")}  ) }}">{{ "now"|date("Y-m-d") }}</a></li>
            <li><a href="{{ path('app_admin_statistique') }}">Statistique</a></li>
            <li><a href="{{ path('app_admin_transfertbdd') }}">TransfertBdd</a></li>
            <li><a href="{{ path('app_admin_code') }}">Code</a></li>
            <li><a href="{{ path('easy_admin') }}">Easy</a></li>
            <li><a href="{{ path('default') }}">Accueil</a></li>
            <li><a href="{{ path('app_logout') }}"><img alt="Logout" src="{{ asset('icons/logout.svg') }}"></a></li>
        </ul>
    </nav>
</header>
<main>
    {% block body %}{% endblock %}
</main>
</body>
</html>
